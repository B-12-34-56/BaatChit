{
  "name": "block-image-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for duplicate image detection using perceptual hashing",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "zip -r function.zip . -x '*.git*' 'node_modules/.cache/*'",
    "install-deps": "npm ci --omit=dev",
    "deploy": "npm run install-deps && npm run zip"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.547.0",
    "@aws-sdk/client-dynamodb": "^3.547.0",
    "@aws-sdk/lib-dynamodb": "^3.547.0",
    "sharp": "^0.32.5"
  },
  "devDependencies": {},
  "keywords": [
    "aws",
    "lambda",
    "image",
    "duplicate",
    "perceptual-hashing",
    "block-image"
  ],
  "author": "",
  "license": "ISC"
} 